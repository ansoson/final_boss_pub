package engine.UIKit;

/**
 * Specifies the horizontal alignment of UIObject in relation to the object's parent, or,
 * if no parent, the alignment in relation to the screen.
 */
public enum HAlignment {
  LEFT, RIGHT, CENTER
}
